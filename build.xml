<project>
    <property name="dummy" value="Before running this task you need to expand core with ./gradlew expandCore" />
    <target name="setup">
        <fail>${dummy}</fail>
    </target>
    <target name="install.source">
        <fail>${dummy}</fail>
    </target>
</project>
