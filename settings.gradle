pluginManagement {
    repositories {
        maven {
            url 'https://repo.futit.cloud/repository/maven-releases/'
            credentials {
                username "$nexusUser"
                password "$nexusPassword"
            }
        }
    }
}

rootProject.name = "etendo"
